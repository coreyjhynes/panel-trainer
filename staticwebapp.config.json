{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/*.css", "/*.js", "/*.md"]
  },
  "routes": [
    { "route": "/api/scores", "methods": ["GET", "POST", "DELETE", "OPTIONS"], "allowedRoles": ["anonymous"] }
  ],
  "mimeTypes": {
    ".md": "text/markdown",
    ".json": "application/json"
  }
}
